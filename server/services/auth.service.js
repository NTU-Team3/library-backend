const mongoose = require("mongoose");
const Member = require("../model/Member");

module.exports = {
  login: async () => {
    const result = {
      status: null,
      message: null,
      data: null,
    };
  },
};
